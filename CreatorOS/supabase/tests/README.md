# Supabase pgTAP Tests

## Purpose

RLS tenant-isolation test suite run directly against Postgres alongside migrations. Verifies allow/deny behavior for every exposed tenant table across SELECT, INSERT, UPDATE, DELETE, and UPSERT paths.

## Key Documents

- `docs/testing/integration-test-cases.md` §5 for required test cases (INT-RLS-01 through INT-RLS-08)

## Running

<!-- Placeholder: pg_prove command against local/test Supabase instance -->
