/**
 * Fastify HTTP server entry point.
 *
 * Responsibilities:
 * - Register plugins (auth, error handler, observability)
 * - Register route modules (content, connections, search, operations)
 * - Start listening on configured port
 *
 * Derived from: docs/tdd/TDD-03-public-api-bff.md section 8.1
 */
